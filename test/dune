(executables
    (libraries bimage)
    (names test)
    (ocamlopt_flags -S)
    (modules test))

(alias
    (name runtest)
    (package bimage)
    (deps test.exe test.jpg)
    (action (run ${<} test.jpg)))
