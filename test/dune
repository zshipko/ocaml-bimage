(executables
    (libraries bimage)
    (names test)
    (ocamlopt_flags -O3 -S)
    (modules test))

(alias
    (name runtest)
    (package bimage)
    (deps test.exe test.jpg)
    (action (run %{dep:test.exe} test.jpg)))
